# TrendRadar 架构理解评测题（100 分版）

给模型的统一题目如下（请整段复制给模型）：

---

你正在回答 TrendRadar 项目的架构理解小测。请阅读以下节选并按要求作答。

**架构节选要点**：
1) 用户触点层包含：React 前端（frontend/，端口 5173）、CLI 命令行（main.py）、MCP Server（mcp_server/）。  
2) 后端为 FastAPI（server.py），架构图标注端口 8000 / 5173。  
3) 主要 API：  
   - `/api/news/{category}`（新闻）  
   - `/api/data`（大宗商品）  
   - `/api/generate-analysis`（AI 分析）  
   - `/api/market-analysis`（市场概况）  
   - `/api/price-history`（价格历史）  
   - `/api/reports`（报告管理）  
   - `/api/cache/status`（缓存状态）  
4) 数据层：Redis（缓存+历史，端口 49907，新闻缓存 TTL=1 小时=3600 秒）、MySQL（commodity_latest / commodity_history）、文件系统（reports/*.md 与 output/*.json）。  
5) scrapers/ 核心文件：unified.py, finance.py, commodity.py, smm.py, plastic21cp.py, plasway.py。  
6) pacong/ 组件：browser/ 下 applescript.py, selenium_driver, cdp_driver；scrapers/ 下 business_insider, world_bank。  
7) core/ 组件：config.py, analyzer.py, statistics.py, price_history.py, notifiers/, reporters/。  
8) 大宗商品来源：新浪期货、上海有色网、Business Insider、中塑在线 21CP、中国原油网 intercrude、WTI 原油。  
9) AI 配置：  
   - internal.api_base = http://10.180.116.2:6400/v1  
   - internal.model = openai_gpt-oss-120b  
   - external.api_base = https://generativelanguage.googleapis.com/v1beta  
   - external.model = gemini-3-pro-preview  
   - external.thinking_level = high  
10) 权重配置：rank_weight=0.6, frequency_weight=0.3, hotness_weight=0.1。  
11) 前端区域折线图颜色：华东 #3b82f6，华南 #10b981，华北 #f59e0b。  

**作答要求（必须严格按 JSON 输出，不要额外解释）**：
```json
{
  "q1_touchpoints": ["...","...","..."],
  "q2_ports": {
    "frontend": 5173,
    "backend": [8000, 5173],
    "redis": 49907
  },
  "q3_api_endpoints": ["..."],
  "q4_route_files": {
    "/api/news/{category}": "api/routes/news.py",
    "/api/data": "api/routes/data.py"
  },
  "q5_scrapers_files": ["..."],
  "q6_pacong_components": {
    "browser": ["..."],
    "scrapers": ["..."]
  },
  "q7_core_modules": ["..."],
  "q8_commodity_sources": ["..."],
  "q9_persistence_targets": ["..."],
  "q10_ai_config": {
    "internal_api_base": "...",
    "internal_model": "...",
    "external_api_base": "...",
    "external_model": "...",
    "thinking_level": "high"
  },
  "q11_weight_values": {
    "rank_weight": 0.6,
    "frequency_weight": 0.3,
    "hotness_weight": 0.1
  },
  "q12_region_colors": {
    "华东": "#3b82f6",
    "华南": "#10b981",
    "华北": "#f59e0b"
  }
}
```

**回答后自跑分**：
1) 把你刚输出的 JSON 原样粘贴到下面 EOF 中  
2) 执行命令并把输出的 JSON 原样返回  

```bash
python3 scripts/score_arch_exam_100.py --json <<'EOF'
（粘贴你的答案 JSON）
EOF
```

如果你无法执行命令，请说明“无法跑分”，只返回 JSON 答案。

---
